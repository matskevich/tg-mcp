# ИНСТРУКЦИЯ: Переименование S16-Leads → TGflow

**Date:** 2025-12-16  
**Status:** Step-by-step guide

---

## 🎯 ЦЕЛЬ

Переименовать проект из **S16-Leads** в **TGflow** для отражения инфраструктурной природы проекта.

---

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Подготовка (локально)

```bash
# 1. Убедитесь, что все изменения закоммичены
git status

# 2. Создайте ветку для переименования
git checkout -b rename-to-tgflow

# 3. Убедитесь, что у вас есть backup (опционально)
git tag backup-before-rename
```

### Шаг 2: Переименование репозитория на GitHub

1. Откройте репозиторий на GitHub
2. Перейдите в **Settings** → **General**
3. Прокрутите до секции **Repository name**
4. Измените имя с `s16-leads` на `tgflow`
5. Нажмите **Rename**

**Важно:** GitHub автоматически перенаправит старые URL на новые.

### Шаг 3: Обновление remote URL

```bash
# Проверьте текущий remote
git remote -v

# Обновите URL (замените USERNAME на ваш)
git remote set-url origin https://github.com/USERNAME/tgflow.git

# Проверьте
git remote -v
```

### Шаг 4: Обновление кода

**Автоматическая замена (осторожно!):**

```bash
# Найти все файлы с упоминанием S16-Leads
grep -r "S16-Leads" --include="*.md" --include="*.py" --include="*.toml" .

# Заменить в файлах (пример для README.md)
sed -i '' 's/S16-Leads/TGflow/g' README.md
```

**Ручная замена (рекомендуется):**

1. **README.md** — обновить заголовок и описание
2. **PROJECT_SUMMARY_FOR_GPT.md** — переписать описание
3. **docs/** — обновить все документы
4. **.cursor/rules/** — обновить правила
5. **packages/tg_core/pyproject.toml** — обновить метаданные

### Шаг 5: Проверка изменений

```bash
# Проверить, что изменилось
git status
git diff

# Убедиться, что тесты проходят
python3 -m pytest tests/
```

### Шаг 6: Коммит и push

```bash
# Добавить все изменения
git add .

# Коммит
git commit -m "Rename project from S16-Leads to TGflow

- Update project name throughout codebase
- Rename repository to reflect infrastructure nature
- Update documentation and README
- Keep legacy apps/s16leads for backward compatibility"

# Push в ветку
git push origin rename-to-tgflow
```

### Шаг 7: Merge в main

```bash
# Переключиться на main
git checkout main

# Merge ветки
git merge rename-to-tgflow

# Push в main
git push origin main

# Удалить ветку (опционально)
git branch -d rename-to-tgflow
git push origin --delete rename-to-tgflow
```

---

## 🔍 ЧТО ОБНОВИТЬ В КОДЕ

### Критичные файлы:

- [ ] `README.md` — главное описание
- [ ] `PROJECT_SUMMARY_FOR_GPT.md` — описание для GPT
- [ ] `packages/tg_core/pyproject.toml` — метаданные пакета
- [ ] `.cursor/rules/60-arch-current.md` — архитектура
- [ ] Все файлы в `docs/` с упоминанием S16-Leads

### Опционально:

- [ ] Комментарии в коде
- [ ] Примеры в `examples/`
- [ ] Скрипты в `scripts/`

### НЕ менять:

- `apps/s16leads/` — можно оставить как legacy
- Логику работы с группами S16 (если нужна)
- Данные в `data/`

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **GitHub автоматически перенаправит:**
   - Старые URL будут работать
   - Клоны через старый URL продолжат работать

2. **Зависимости:**
   - Если кто-то использует проект, уведомите их
   - Обновите документацию для пользователей

3. **CI/CD:**
   - Проверьте `.github/workflows/` — возможно, нужно обновить

4. **Backup:**
   - Создайте тег перед переименованием
   - Убедитесь, что все закоммичено

---

## ✅ ЧЕКЛИСТ

- [ ] Создана ветка `rename-to-tgflow`
- [ ] Репозиторий переименован на GitHub
- [ ] Remote URL обновлён
- [ ] Код обновлён (README, docs, etc.)
- [ ] Тесты проходят
- [ ] Изменения закоммичены
- [ ] Ветка смержена в main
- [ ] Push выполнен

---

## 🆘 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### Откат изменений:

```bash
# Откатить последний коммит (локально)
git reset --soft HEAD~1

# Откатить изменения в файле
git checkout -- README.md

# Вернуться на main
git checkout main
```

### Восстановление репозитория на GitHub:

1. Settings → General → Repository name
2. Вернуть старое имя `s16-leads`
3. Или создать новый репозиторий и push туда

---

**Created:** 2025-12-16



