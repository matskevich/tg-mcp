# PROJECT MAP & RENAME PLAN: S16-Leads → TGflow

**Date:** 2025-12-16  
**Status:** Planning document for discussion with GPT  
**Purpose:** Map current project structure and plan renaming to TGflow

---

## 🎯 ЧТО ЭТО ЗА ПРОЕКТ

### Текущее название: S16-Leads
### Целевое название: **TGflow**

**Суть проекта:** Инфраструктура для работы с Telegram через Telethon (MTProto).  
**Основное назначение:** Создание инструментов для извлечения данных из Telegram и синхронизации с другими проектами.

Это **не** про лиды или конкретный бизнес-кейс.  
Это про **инфраструктуру** — переиспользуемый набор инструментов для работы с Telegram API.

---

## 🏗️ АРХИТЕКТУРА ПРОЕКТА

### Модель: Монорепо с единым ядром

```
TGflow (бывший S16-Leads)
├── packages/tg_core/          # Ядро — переиспользуемая инфраструктура
│   ├── tg_core/infra/         # Инфраструктура: клиент, лимитер, метрики
│   ├── tg_core/domain/        # Доменные модели: группы, участники
│   └── tg_core/config/        # Конфигурация
│
├── apps/                       # Приложения, использующие ядро
│   ├── s16leads/              # S16-специфичная логика (legacy)
│   ├── gconf/                 # GConf аналитика и отчёты
│   ├── kuprianov/             # Проект Kuprianov
│   ├── vahue/                 # Проект Vahue
│   └── wildtantra/            # Проект WildTantra
│
├── examples/                   # Примеры использования
├── scripts/                    # Утилиты и скрипты
├── tests/                      # Тесты
└── docs/                       # Документация
```

### Ключевые принципы:

1. **Единое ядро (`tg_core`):**
   - Все обращения к Telegram API — только через `tg_core`
   - Встроенная антиспам-защита (rate limiting, 4 RPS)
   - Безопасное управление сессиями
   - Общий лимитер на всю сессию

2. **Изолированные приложения (`apps/*`):**
   - Каждое приложение использует только публичный API `tg_core`
   - Приложения не импортируют друг друга
   - Можно вынести в отдельный репозиторий с зависимостью от `tg_core`

3. **Инварианты:**
   - Никаких прямых вызовов Telethon в `apps/*`
   - `tg_core` не импортирует `apps/*`
   - PII и секреты не коммитятся

---

## 📦 КОМПОНЕНТЫ ЯДРА (`tg_core`)

### `tg_core/infra/`
- **`tele_client.py`** — управление Telegram сессиями, безопасная аутентификация
- **`limiter.py`** — rate limiting (4 RPS), token bucket, защита от FLOOD_WAIT
- **`metrics.py`** — сбор метрик использования API
- **`logging.py`** — структурированное логирование

### `tg_core/domain/`
- **`groups.py`** — `GroupManager`: получение участников, поиск, экспорт, информация о группах

### `tg_core/config/`
- **`loader.py`** — загрузка конфигурации из `.env`, валидация

---

## 🔧 ПРИЛОЖЕНИЯ / ПРОЕКТЫ

### `apps/s16leads/` (legacy, можно переименовать или удалить)
- CLI для работы с группами S16
- Сверка участников между группами
- Анализ пересечений

### `gconf/`
- Проект gconf: docs + код (CLI/tools/pipelines) + локальные данные/экспорты (gitignored)
- Telegram-утилиты: `gconf/src/*`
- Samples/config: `gconf/app/*`

### `apps/kuprianov/`, `apps/vahue/`, `apps/wildtantra/`
- Специфичная логика для каждого проекта
- Используют общее ядро `tg_core`

---

## 🎯 ПОЧЕМУ ПЕРЕИМЕНОВАНИЕ В TGflow

### Текущее название "S16-Leads" не отражает суть:
- ❌ Ассоциируется с конкретным бизнес-кейсом (лиды S16)
- ❌ Не отражает инфраструктурную природу проекта
- ❌ Не показывает, что это переиспользуемый инструментарий

### Новое название "TGflow" отражает:
- ✅ **TG** = Telegram
- ✅ **flow** = поток данных, инфраструктура для работы с данными
- ✅ Нейтральное, не привязано к конкретному проекту
- ✅ Понятно, что это про работу с Telegram

---

## 📋 ПЛАН ПЕРЕИМЕНОВАНИЯ

### Этап 1: Подготовка (локально)

1. **Создать ветку для переименования:**
   ```bash
   git checkout -b rename-to-tgflow
   ```

2. **Переименовать файлы и директории:**
   - `s16-leads/` → `tgflow/` (локально, до push)
   - Обновить все упоминания в коде

3. **Обновить конфигурационные файлы:**
   - `README.md` — описание проекта
   - `packages/tg_core/pyproject.toml` — имя пакета (если нужно)
   - `.cursor/rules/*` — обновить правила
   - `docs/*` — обновить документацию

### Этап 2: Обновление кода

**Файлы, где нужно заменить упоминания:**

1. **README.md** — главное описание проекта
2. **PROJECT_SUMMARY_FOR_GPT.md** — описание для GPT
3. **docs/** — все документы с упоминанием S16-Leads
4. **apps/s16leads/** — можно переименовать в `apps/legacy/` или оставить
5. **.cursor/rules/** — правила архитектуры
6. **Makefile** — если есть упоминания
7. **pyproject.toml** — метаданные пакета

**Что НЕ менять:**
- `apps/s16leads/` — можно оставить как есть (это legacy приложение)
- Логику работы с группами S16 (если она нужна)
- Данные в `data/` (они специфичны для проектов)

### Этап 3: GitHub

1. **Переименовать репозиторий на GitHub:**
   - Settings → General → Repository name
   - `s16-leads` → `tgflow`

2. **Обновить remote URL:**
   ```bash
   git remote set-url origin https://github.com/USERNAME/tgflow.git
   ```

3. **Обновить описание репозитория:**
   - "Telegram infrastructure toolkit for data extraction and synchronization"

### Этап 4: Коммит и push

```bash
# Проверить изменения
git status
git diff

# Добавить все изменения
git add .

# Коммит
git commit -m "Rename project from S16-Leads to TGflow

- Update project name throughout codebase
- Rename repository to reflect infrastructure nature
- Update documentation and README
- Keep legacy apps/s16leads for backward compatibility"

# Push в новую ветку
git push origin rename-to-tgflow

# После проверки — merge в main
git checkout main
git merge rename-to-tgflow
git push origin main
```

---

## 🔍 ЧТО НУЖНО ОБНОВИТЬ

### Критичные файлы:

1. **README.md**
   - Заголовок: `# TGflow` (вместо `# S16-Leads`)
   - Описание: "Telegram infrastructure toolkit"
   - Убрать упоминания про "лиды" и "S16"

2. **packages/tg_core/pyproject.toml**
   - `name = "tg_core"` (оставить как есть — это правильно)
   - `description` — обновить на "Telegram core infrastructure"
   - `authors` — убрать "S16", если не нужно

3. **.cursor/rules/60-arch-current.md**
   - Обновить описание проекта
   - Убрать упоминания S16-Leads

4. **docs/PROJECT_SUMMARY_FOR_GPT.md**
   - Полностью переписать под TGflow

### Опционально:

- `apps/s16leads/` — можно переименовать в `apps/legacy/` или оставить
- Все примеры в `examples/` — обновить комментарии
- Документация в `docs/` — обновить упоминания

---

## ⚠️ РИСКИ И ВОПРОСЫ ДЛЯ ОБСУЖДЕНИЯ

### Вопросы:

1. **Что делать с `apps/s16leads/`?**
   - Оставить как legacy?
   - Переименовать в `apps/legacy/`?
   - Удалить, если не используется?

2. **Нужно ли менять имя пакета `tg_core`?**
   - Сейчас: `tg_core`
   - Предложение: оставить как есть (это правильно)

3. **Как обрабатывать историю Git?**
   - Оставить как есть (GitHub автоматически перенаправит)
   - Или создать новый репозиторий?

4. **Нужно ли обновлять все упоминания "S16" в коде?**
   - В комментариях и документации — да
   - В логике работы с группами S16 — нет (если это нужно)

### Риски:

- **Потеря ссылок:** GitHub автоматически перенаправит старые URL
- **Зависимости:** Если кто-то использует проект, нужно уведомить
- **CI/CD:** Обновить workflows в `.github/workflows/`

---

## 📝 ЧЕКЛИСТ ПЕРЕИМЕНОВАНИЯ

### Подготовка:
- [ ] Создать ветку `rename-to-tgflow`
- [ ] Сделать backup текущего состояния
- [ ] Проверить, что все тесты проходят

### Обновление кода:
- [ ] Обновить `README.md`
- [ ] Обновить `PROJECT_SUMMARY_FOR_GPT.md`
- [ ] Обновить документацию в `docs/`
- [ ] Обновить `.cursor/rules/`
- [ ] Обновить `packages/tg_core/pyproject.toml`
- [ ] Обновить комментарии в коде
- [ ] Проверить `Makefile` и скрипты

### GitHub:
- [ ] Переименовать репозиторий на GitHub
- [ ] Обновить remote URL
- [ ] Обновить описание репозитория
- [ ] Обновить topics/tags

### Финальная проверка:
- [ ] Все тесты проходят
- [ ] Документация актуальна
- [ ] Нет битых ссылок
- [ ] Коммит и push выполнены

---

## 🎯 ИТОГОВОЕ ОПИСАНИЕ ПРОЕКТА (для README)

**TGflow** — инфраструктура для работы с Telegram через Telethon (MTProto).

**Основные возможности:**
- Безопасный доступ к Telegram API с встроенной антиспам-защитой
- Управление группами и участниками
- Извлечение и экспорт данных
- Переиспользуемое ядро для создания собственных инструментов

**Архитектура:**
- `tg_core` — переиспользуемое ядро (rate limiting, безопасные сессии, доменные модели)
- `apps/*` — приложения, использующие ядро
- Монорепо структура для удобного развития

**Для кого:**
Разработчики, которым нужно:
- Извлекать данные из Telegram групп
- Синхронизировать данные с другими проектами
- Создавать инструменты для работы с Telegram

---

## 📚 СЛЕДУЮЩИЕ ШАГИ

1. Обсудить план с GPT для уточнения деталей
2. Создать детальный список всех файлов для замены
3. Выполнить переименование по этапам
4. Обновить документацию
5. Закоммитить изменения

---

**Created:** 2025-12-16  
**Status:** Ready for discussion and implementation




