# 📥 Руководство по выгрузке переписки группы

## 🎯 Быстрый старт

Выгрузить переписку группы "shipyard cohort 1":

```bash
python examples/export_group_messages.py "shipyard cohort 1"
```

Или по ID/username:

```bash
python examples/export_group_messages.py -1001234567890
python examples/export_group_messages.py @groupname
```

## 📋 Параметры

```bash
python examples/export_group_messages.py GROUP [OPTIONS]

Аргументы:
  GROUP              ID группы, username или название

Опции:
  -o, --output FILE  Путь к файлу вывода
                     (по умолчанию: data/export/messages_<group>.json)
  -l, --limit N      Максимальное количество сообщений
                     (по умолчанию: все сообщения)
  --min-id ID        Минимальный ID сообщения (для продолжения выгрузки)
```

## 🛡️ Безопасность

Скрипт использует антиспам защиту:
- ✅ Все API вызовы через `safe_call`
- ✅ Rate limiting (4 запроса/сек)
- ✅ Автоматический retry при FLOOD_WAIT
- ✅ Smart pause каждые 1000 сообщений
- ✅ Защита от превышения лимитов Telegram

## 📊 Формат вывода

Данные сохраняются в JSON:

```json
{
  "group": {
    "id": -1001234567890,
    "title": "shipyard cohort 1",
    "export_date": "2025-01-15T10:30:00",
    "total_messages": 1234,
    "min_id": 0,
    "last_message_id": 5678
  },
  "messages": [
    {
      "id": 1,
      "date": "2025-01-01T10:00:00",
      "from_id": 123456789,
      "text": "Текст сообщения",
      "is_reply": false,
      "reply_to_msg_id": null,
      "views": 100,
      "forwards": 5,
      "is_pinned": false,
      "has_media": false,
      "media_type": null
    }
  ]
}
```

## 🔍 Примеры использования

### Выгрузить все сообщения группы

```bash
python examples/export_group_messages.py "shipyard cohort 1"
```

### Выгрузить только последние 1000 сообщений

```bash
python examples/export_group_messages.py "shipyard cohort 1" --limit 1000
```

### Выгрузить в конкретный файл

```bash
python examples/export_group_messages.py "shipyard cohort 1" -o data/export/shipyard_messages.json
```

### Продолжить выгрузку с определенного ID

```bash
python examples/export_group_messages.py "shipyard cohort 1" --min-id 5000
```

## 📈 Статистика

После выгрузки скрипт показывает:
- Количество выгруженных сообщений
- Время выполнения
- Скорость выгрузки (msg/sec)
- Размер файла
- Статистику антиспам системы

## ⚠️ Ограничения

1. **Большие группы**: Выгрузка может занять много времени
   - Используйте `--limit` для ограничения количества
   - Smart pause автоматически замедляет процесс

2. **FLOOD_WAIT**: Если Telegram вернет FLOOD_WAIT
   - Скрипт автоматически подождет и повторит запрос
   - Не прерывайте выполнение

3. **Память**: Для очень больших групп (>100K сообщений)
   - Все сообщения загружаются в память
   - Рассмотрите батчинг для очень больших групп

## 🎨 Использование данных для метрик

После выгрузки можно использовать данные для анализа:

```python
import json
from datetime import datetime
from collections import Counter

# Загружаем данные
with open('data/export/messages_shipyard_cohort_1.json', 'r') as f:
    data = json.load(f)

messages = data['messages']

# Примеры метрик:
# - Количество сообщений по дням
# - Самые активные участники
# - Средняя длина сообщений
# - Количество ответов
# - Пики активности по времени
```

## 🔧 Устранение проблем

### Группа не найдена

Если группа не найдена по ID, скрипт попытается найти по названию:
- Убедитесь что название написано правильно
- Проверьте что у вас есть доступ к группе

### Медленная выгрузка

Это нормально для больших групп:
- Smart pause замедляет процесс для безопасности
- Rate limiting ограничивает скорость до 4 запросов/сек
- Не прерывайте выполнение

### FLOOD_WAIT ошибки

Скрипт автоматически обрабатывает FLOOD_WAIT:
- Автоматический retry с exponential backoff
- Не нужно ничего делать вручную

## 📝 Примечания

- Скрипт пропускает служебные сообщения (без текста и медиа)
- Сообщения выгружаются от старых к новым (reverse=False)
- Данные сохраняются в UTF-8 для поддержки эмодзи и кириллицы

